{
  "name": "fetch_top_pages",
  "description": "Fetch and read multiple webpages at once. More efficient than calling read_webpage multiple times. Use after web_search to get detailed content from the best results.",
  "parameters": {
    "type": "object",
    "properties": {
      "urls": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Array of URLs to fetch (max 5)"
      }
    },
    "required": ["urls"]
  }
}
